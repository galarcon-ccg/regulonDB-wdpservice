requisitos
- entorno virtual (venv)
 to install
 sudo apt install python3-venv

dependencias
- flask
- "graphene>=3.0"
- flask-cors
- csv
to install on venv
(venv) 
pip install flask flask-cors "graphene>=3.0"

